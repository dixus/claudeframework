# Scout Report — 2026-03-20

## Claude Code release notes (last 60 days)

37 releases from v2.1.19 (Jan 23) through v2.1.80 (Mar 19). Key changes affecting our framework:

| Version | Date | Change | Affects our framework? | Action needed |
|---|---|---|---|---|
| 2.1.80 | Mar 19 | `effort` frontmatter for skills/slash commands | All 18 skills | Add `effort` field to computationally intensive skills (e.g., `/2_review`, `/ship`) |
| 2.1.80 | Mar 19 | `--channels` (research preview) — MCP servers push messages into session | `/smoke`, `/healthcheck` | Monitor — could replace polling patterns in smoke/healthcheck |
| 2.1.78 | Mar 17 | `StopFailure` hook event | `/ship`, `/3_fix` | Could add circuit breaker hooks for API errors during pipeline |
| 2.1.78 | Mar 17 | `maxTurns`, `disallowedTools` frontmatter for agents | All skills using subagents | Add `maxTurns` to prevent runaway subagents in `/ship`, `/test` |
| 2.1.76 | Mar 14 | MCP elicitation support + `Elicitation` hook events | None currently | New capability for interactive MCP workflows |
| 2.1.76 | Mar 14 | `PostCompact` hook event | `/handoff` | Could auto-save handoff state on compaction |
| 2.1.76 | Mar 14 | `/effort` slash command | None | Users can now set effort level — document in framework docs |
| 2.1.75 | Mar 13 | 1M context window for Opus 4.6 (Max/Team/Enterprise) | All skills | Reduces compaction pressure — update docs to note this |
| 2.1.74 | Mar 12 | `/context` command with suggestions | None | Useful for debugging context bloat — document for users |
| 2.1.72 | Mar 10 | `ExitWorktree` tool; `model` parameter restored on Agent tool | `/ship`, `/1_implement` | Can now use worktree isolation in pipeline |
| 2.1.72 | Mar 10 | Effort levels simplified to low/medium/high (removed max) | All skills | Update any references if we add `effort` frontmatter |
| 2.1.69 | Mar 5 | `${CLAUDE_SKILL_DIR}` variable for skills | All 18 skills | Use for portable file references within skill directories |
| 2.1.69 | Mar 5 | `InstructionsLoaded` hook event | `/continue` | Could trigger post-load actions after CLAUDE.md loads |
| 2.1.69 | Mar 5 | `agent_id` and `agent_type` added to hook events | Hooks | Better observability for subagent hooks |
| 2.1.63 | Feb 28 | `/simplify` and `/batch` bundled slash commands | Overlaps with our `/2_review` | Our review is more comprehensive — no action needed |
| 2.1.63 | Feb 28 | HTTP hooks (POST JSON to URL) | `/create-hook` | Update `/create-hook` to support HTTP hook scaffolding |
| 2.1.63 | Feb 28 | Project configs shared across git worktrees | `/ship` | Enables worktree-based parallel implementation |
| 2.1.59 | Feb 26 | Auto-memory: Claude saves useful context automatically | Our manual memory system | Built-in now — evaluate overlap with our handoff/lessons approach |
| 2.1.50 | Feb 20 | `WorktreeCreate` and `WorktreeRemove` hook events | `/ship` | Could add hooks for worktree lifecycle during pipeline |
| 2.1.50 | Feb 20 | `isolation: worktree` in agent definitions | `/ship`, `/1_implement` | Run implementation in isolated worktree for safety |
| 2.1.50 | Feb 20 | `claude agents` CLI to list agents | None | Informational — document for users |
| 2.1.49 | Feb 19 | `background: true` in agent definitions | `/ship` | Run review as background agent while fix continues |
| 2.1.33 | Feb 6 | `memory` frontmatter for agents (user/project/local scope) | `/2_review`, `/debug` | Add persistent memory to review agent for cross-session learning |
| 2.1.33 | Feb 6 | `TeammateIdle` and `TaskCompleted` hook events | None | Relevant if we adopt agent teams |
| 2.1.33 | Feb 6 | `Task(agent_type)` syntax for restricting spawnable sub-agents | `/ship` | Can restrict which subagents `/ship` spawns |
| 2.1.32 | Feb 5 | **Opus 4.6 released** | All skills | Already using — no action |
| 2.1.32 | Feb 5 | Agent Teams (research preview) | `/ship` | Could orchestrate spec→implement→review as agent team |
| 2.1.32 | Feb 5 | Skills in `--add-dir` automatically loaded | Framework distribution | Document for users adopting framework in monorepos |
| 2.1.30 | Feb 3 | `pages` parameter for PDF Read tool | None | Not relevant to current framework |
| 2.1.19 | Jan 23 | Skills without permissions/hooks allowed without approval | All 18 skills | Already using `disable-model-invocation` — confirm alignment |

## New features we're not using

| Feature | Source | Impact | Recommendation |
|---|---|---|---|
| `effort` frontmatter | [Changelog v2.1.80](https://code.claude.com/docs/en/changelog) | **High** | Add `effort: high` to `/2_review`, `/ship`, `/0_spec`; `effort: medium` to `/commit`, `/test`; `effort: low` to `/doc` |
| `${CLAUDE_SKILL_DIR}` variable | [Changelog v2.1.69](https://code.claude.com/docs/en/changelog) | **High** | Replace hardcoded `.claude/skills/<name>/` paths with `${CLAUDE_SKILL_DIR}` for portability |
| `isolation: worktree` for subagents | [Subagents docs](https://code.claude.com/docs/en/sub-agents) | **High** | Use in `/ship` and `/1_implement` so implementation runs in an isolated worktree, preventing WIP from polluting main branch |
| `memory` frontmatter for agents | [Subagents docs](https://code.claude.com/docs/en/sub-agents) | **Medium** | Create `.claude/agents/reviewer.md` with `memory: project` so review insights persist across sessions |
| Agent Teams | [Agent Teams docs](https://code.claude.com/docs/en/agent-teams) | **Medium** | Could replace `/ship`'s sequential pipeline with parallel agent team (spec writer + implementer + reviewer). Still research preview — monitor |
| HTTP hooks | [Changelog v2.1.63](https://code.claude.com/docs/en/changelog) | **Medium** | Enable webhook notifications for pipeline events (e.g., POST to Slack when `/ship` completes). Update `/create-hook` skill |
| `maxTurns` frontmatter | [Changelog v2.1.78](https://code.claude.com/docs/en/changelog) | **Medium** | Add `maxTurns` to subagent invocations in `/ship` and `/test` to prevent runaway agents |
| `disallowedTools` frontmatter | [Subagents docs](https://code.claude.com/docs/en/sub-agents) | **Low** | Could restrict `/2_review` subagent from Write/Edit tools (read-only review) |
| `background: true` for agents | [Subagents docs](https://code.claude.com/docs/en/sub-agents) | **Low** | Run long tasks (test suite, build) as background agents in `/ship` |
| `PostCompact` hook | [Changelog v2.1.76](https://code.claude.com/docs/en/changelog) | **Low** | Auto-trigger `/handoff` on compaction to preserve session state |
| `skills` field in agent definitions | [Subagents docs](https://code.claude.com/docs/en/sub-agents) | **Low** | Preload relevant skills into subagents instead of relying on discovery |

## Skills that could be improved

| Skill | Insight | Source | Suggested change |
|---|---|---|---|
| `/ship` | Agent Teams allow parallel spec→implement→review with inter-agent messaging | [Agent Teams guide](https://claudefa.st/blog/guide/agents/agent-teams) | Add optional `--parallel` flag that uses agent teams instead of sequential pipeline |
| `/ship` | Worktree isolation prevents WIP from polluting the working directory | [Subagents docs — isolation](https://code.claude.com/docs/en/sub-agents) | Wrap implementation phase in `isolation: worktree` |
| `/2_review` | Subagents can have persistent `memory` that accumulates review patterns | [Subagents docs — memory](https://code.claude.com/docs/en/sub-agents) | Create `.claude/agents/reviewer.md` with `memory: project` and migrate review logic |
| `/1_implement` | `effort: high` frontmatter ensures maximum reasoning for complex implementations | [Changelog v2.1.80](https://code.claude.com/docs/en/changelog) | Add `effort: high` to skill frontmatter |
| `/create-hook` | HTTP hooks now supported — POST JSON to URLs | [Changelog v2.1.63](https://code.claude.com/docs/en/changelog) | Update hook scaffolding to offer HTTP hook type alongside command hooks |
| `/test` | `maxTurns` can prevent test runner from spiraling | [Subagents docs](https://code.claude.com/docs/en/sub-agents) | Set `maxTurns: 30` on test subagent invocations |
| `/commit` | Official best practices emphasize hooks for enforcement, not just CLAUDE.md rules | [Best practices](https://code.claude.com/docs/en/best-practices) | Consider adding a `PreToolUse` hook that validates conventional commit format |
| `/doc` | `${CLAUDE_SKILL_DIR}` enables portable references | [Changelog v2.1.69](https://code.claude.com/docs/en/changelog) | Replace all hardcoded skill directory paths |
| `/handoff` | `PostCompact` hook can auto-trigger handoff | [Changelog v2.1.76](https://code.claude.com/docs/en/changelog) | Add optional auto-handoff hook that fires on compaction |
| `/scout` | Official Anthropic skills repo exists as plugin | [github.com/anthropics/skills](https://github.com/anthropics/skills) | Reference in scout searches for official patterns |
| All skills | Skill authoring best practices recommend `description` includes both what + when | [Skill best practices](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices) | Audit all 18 skill descriptions to include trigger conditions |
| All skills | Best practices recommend SKILL.md body under 500 lines | [Skill best practices](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices) | Audit skill file lengths; split large ones using progressive disclosure |

## Breaking changes / deprecations

| Change | Affects | Source | Action needed |
|---|---|---|---|
| Removed `/output-style` command (v2.1.75) | None | [Changelog](https://code.claude.com/docs/en/changelog) | No action — we don't use this |
| Removed Opus 4 and 4.1 from first-party API (v2.1.68) | None | [Changelog](https://code.claude.com/docs/en/changelog) | No action — already on Opus 4.6 |
| Removed deprecated Windows managed settings fallback (v2.1.77) | None | [Changelog](https://code.claude.com/docs/en/changelog) | No action |
| Effort levels simplified to low/medium/high — removed `max` (v2.1.72) | Future `effort` frontmatter | [Changelog](https://code.claude.com/docs/en/changelog) | Use only low/medium/high when adding `effort` fields |
| `$ARGUMENTS.0` syntax changed to `$ARGUMENTS[0]` (v2.1.19) | Skills using indexed args | [Changelog](https://code.claude.com/docs/en/changelog) | Audit skills for old syntax — update if found |
| Task tool renamed to Agent (v2.1.63) | Documentation references | [Changelog](https://code.claude.com/docs/en/changelog) | Update any docs referencing "Task tool" to "Agent tool" |

## Worth investigating

Links to save into `.claude/references/` for deeper `/learn` processing:

- [Official Anthropic Skills Repository](https://github.com/anthropics/skills) — reference implementations of skill structure, progressive disclosure, and plugin packaging
- [Skill Authoring Best Practices](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices) — official guidance on description writing, `${CLAUDE_SKILL_DIR}`, progressive disclosure, feedback loops, and token budgets
- [Claude Code Subagents Docs](https://code.claude.com/docs/en/sub-agents) — complete reference for `memory`, `isolation: worktree`, `skills` preloading, `maxTurns`, `disallowedTools`, and hook configuration
- [Claude Code Best Practices](https://code.claude.com/docs/en/best-practices) — official patterns for CLAUDE.md structure, context management, parallel sessions, and fan-out workflows
- [Claude Code Agent Teams Guide](https://claudefa.st/blog/guide/agents/agent-teams) — practical guide to multi-agent orchestration patterns
- [claude-code-hooks-multi-agent-observability](https://github.com/disler/claude-code-hooks-multi-agent-observability) — real-time monitoring for Claude Code agents through hook event tracking
- [Claude Code Ultimate Guide](https://github.com/FlorianBruniaux/claude-code-ultimate-guide) — comprehensive community guide covering features, workflows, and templates
- [awesome-claude-skills](https://github.com/travisvn/awesome-claude-skills) — curated community skills collection for pattern inspiration

## No action needed

Patterns we already implement correctly:

- **Plan mode before implementation** — ✅ already in `/1_implement` (plan approval gate)
- **TDD workflow** — ✅ already in `/1_implement` (red→green→refactor loop)
- **Severity-based review** — ✅ already in `/2_review` (9-lens review with severity classification)
- **Circuit breaker for recurring issues** — ✅ already in `/3_fix` (2+ same issues = escalate)
- **Conventional commits** — ✅ already in `/commit` (with emoji mapping and multi-concern detection)
- **Session state capture** — ✅ already in `/handoff` + `/continue`
- **Learning loop** — ✅ already in `/learn` + `lessons.md` + `/ship` lesson graduation
- **Blast radius analysis** — ✅ already in `/impact` (call sites, mocks, consumers)
- **Read-before-edit instinct** — ✅ already in `instincts.md`
- **Minimal change philosophy** — ✅ already in `instincts.md`
- **Spec-driven development** — ✅ already in `/0_spec` → `/1_implement` pipeline
- **`disable-model-invocation` for side-effect skills** — ✅ already on all manual-invoke skills
- **Progressive disclosure in skill structure** — ✅ skills reference context files on demand
- **Separate scoring engine from UI** — ✅ `src/lib/scoring/` is framework-agnostic
