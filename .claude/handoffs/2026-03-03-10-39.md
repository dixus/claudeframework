# Handoff: framework-restructure-and-new-skills
Date: 2026-03-03-10-39

## Current task
Restructure the repo as a Claude Code framework (not an AI Maturity Score app) and add /handoff, /continue, and instincts.md.

## Pipeline position
Done — all changes implemented, uncommitted. Next: `/commit`.

## Active files
- Spec: None (this was framework meta-work, no spec)
- Review: None
- Key source files:
  - `CLAUDE.md` — rewritten framework-first
  - `README.md` — new GitHub-facing docs
  - `.claude/skills/handoff/SKILL.md` — new skill
  - `.claude/skills/continue/SKILL.md` — new skill
  - `.claude/context/instincts.md` — new file
  - `.claude/docs/README.md` — updated
  - `.claude/docs/skills-reference.md` — updated (added /handoff + /continue docs)
  - `.claude/docs/workflow.md` — updated (handoff pattern + instincts section)

## Decisions made this session
- Keep AI Maturity Score app as demo (not extracted or deleted) — user confirmed
- Usage model: copy `.claude/` into new projects (not submodule, not npm package)
- Switched default branch from `master` to `main` — force-pushed because `origin/main` had a divergent placeholder initial commit
- From the research round: added only the two highest-value missing pieces (handoff + instincts); skipped everything else as overengineering for personal use
- `instincts.md` is technology-agnostic universal rules — TypeScript-specific rules stay in CLAUDE.md
- The `.claude/handoffs/` directory is created but gitignored consideration: currently tracked, which is fine (handoffs are useful context)

## Open questions
- README.md git clone URL on line ~44 uses a placeholder — replace with actual GitHub repo URL once confirmed
- `.claude/handoffs/` directory: decide whether to add to `.gitignore` (personal session files) or keep tracked (useful for teams). Currently untracked by git (no .gitignore entry yet).

## Next step
Run `/commit` — the diff covers two distinct concerns that could be split:
1. `📝 docs: add session handoff and instincts to framework` (new skills + instincts.md + all doc updates)
2. Or commit everything as one: `✨ feat: add /handoff, /continue skills and instincts.md`

Then `git push origin main`.
